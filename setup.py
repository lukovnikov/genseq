from setuptools import setup, find_packages

setup(name="genseq",
      description="genseq",
      author="Sum-Ting Wong",
      author_email="sumting@wo.ng",
      install_requires=[],
      packages=["genseq"],
      )
